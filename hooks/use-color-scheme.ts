/**
 * Custom useColorScheme hook
 * 
 * Re-exports the theme-aware version from ThemeContext.
 * This is a drop-in replacement that respects user's theme preference.
 */

export { useColorScheme } from '../contexts/ThemeContext';
